// Written out rather than linked. An address spelled with "at" and "dot" does
// not match what address harvesters look for, so it needs no encoding, and it
// survives any build step that might decode character references. The cost is
// that it cannot be clicked.
export const emailDisplay = 'joshi.shruti at mila dot quebec';
